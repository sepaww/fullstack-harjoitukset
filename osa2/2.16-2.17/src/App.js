
import Handler from "./handler"
const App = () => {

  return (
    <div>
      <Handler />
    </div>
  )

}

export default App

// error message for already deleted is done with alert due to difficulties in inheritance
// css error messages made for creating, updating and givng empty name to phonebook